    import XCTest
    @testable import FirstSwiftPackage

    final class FirstSwiftPackageTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(FirstSwiftPackage().text, "Hello, World!")
        }
    }
